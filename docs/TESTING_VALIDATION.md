# Testing & Validation Plan

Data: duplicates, missing IDs, invalid dates, non-positive quantities, prices and revenue arithmetic.

SQL: revenue reconciliation, distinct counts, join correctness, segment reconciliation.

UI: app loads, filters update supported views, no-data state is understandable, tables remain readable.

Deployment: public URL works in incognito, assets/data load, no local paths, reload is stable.
