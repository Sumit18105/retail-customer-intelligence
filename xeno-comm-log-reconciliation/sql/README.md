# SQL guide

Run the SQL files against the supplied SQLite database `data/comm_log.db`.

- `01_investigation.sql` documents the exploratory queries and evidence used to build the reconciliation bridge.
- `final_reconciliation.sql` computes the final Finance metric and should return `22` for the supplied dataset.
